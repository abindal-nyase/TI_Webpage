import s from './HotspotPanel.module.css'

export default function HotspotPanel() {
  return (
    <section className={s.root}>
      <div className={s.placeholder}>
        <span className={s.label}>Act 3 — Hotspot Panel</span>
        <p className={s.note}>Building image + 6 clickable service hotspots · Coming soon</p>
      </div>
    </section>
  )
}
